group 'com.yaegar.qutana'
version '1.0.0-SNAPSHOT'

apply plugin: 'java'

apply from: 'test.gradle'
apply from: 'dependencies.gradle'

sourceCompatibility = 1.8
targetCompatibility = 1.8

repositories {
    mavenCentral()
}

task wrapper(type: Wrapper) {
    gradleVersion = '4.2.1'
}